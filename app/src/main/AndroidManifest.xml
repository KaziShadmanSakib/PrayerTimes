<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.prayertimes">
    <!-- API for User Specific Location -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PrayerTimes"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".RamadanActivity_01"
            android:parentActivityName=".Duas" />
        <activity
            android:name=".AboutUsOption"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".SettingsOption"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".QiblaDirectionOption"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".CalendarDateLog"
            android:parentActivityName=".LogActivity" />
        <activity
            android:name=".AllPrayers"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".Duas" />
        <activity android:name=".LogActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".AlertReceiver" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />
    </application>

</manifest>